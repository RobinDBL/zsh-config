# zsh-config
My custom ZSH config. It includes autosuggestions, autocorrect and a custom theme (one of those fancy ones)

## Install

Replace all files under the `custom` folder in `~/.oh-my-zsh/custom` with the files in this repo.
